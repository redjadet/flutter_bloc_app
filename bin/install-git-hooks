#!/usr/bin/env bash
# Install tracked githooks/ for this clone (local core.hooksPath).
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec bash "$SCRIPT_DIR/../tool/install_git_hooks.sh" "$@"
