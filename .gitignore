# Miscellaneous
*.bak
*.class
*.log
firepit-log.txt
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/
figma-sync/
GEMINI.md
.cursor/*
.cursorignore

# Node lockfile at repo root (functions/ has its own)
/package-lock.json

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/lcov.info

# Integration test runner output (per-run summaries)
/artifacts/
artifacts/integration/
# Perf trace artifacts (traceAction report data)
artifacts/perf/

# Golden test failure artifacts
test/failures/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Platform build outputs – all files and subfolders recursively (do not commit)
**/build/**

# Gradle cache (e.g. third_party plugin builds)
**/.gradle/

# Local secrets
assets/config/secrets.json
android/app/google-services.json
ios/Runner/GoogleService-Info.plist
macos/Runner/GoogleService-Info.plist
firebase.json
# Placeholder `lib/firebase_options.dart` is committed so the app runs
# without Firebase. `flutterfire configure` overwrites it; do not commit
# real project config.
third_party/pub/firebase_remote_config/example/lib/firebase_options.dart
INTERVIEW_QA.md

.config/
# Local agent overlays (skills, symlinks, host-only policy); not part of the app build
.agents/
coverage/lcov.base.info

# Agent scorecard one-off exports (keep the rolling log tracked)
analysis/agent_scorecard/scorecard-events-*.jsonl
tmp_test_write.md
.env.android.release
fastlane/keys/*.json
fastlane/report.xml

# Ruby / Bundler (keep installs local, never commit)
.bundle/
vendor/bundle/
vendor/cache/

# Local direnv config (do not commit)
.envrc

# Local agent scorecard log (do not commit)
analysis/agent_scorecard/scorecard-events.jsonl
tmp/*
tasks/*
CLAUDE.md
docs/gstack_integration.md
docs/app_for_small_business.txt
*.original.md

# Python demo local virtualenv (e.g. demos/render_chat_api)
demos/**/.venv/
.codex/hooks.json
skills-lock.json
# Added by code-review-graph
.code-review-graph/
docs/code_graph.pdf
docs/Harness Engineering.pdf
docs/Harness_Engineering_Leveraging_Codex_OpenAI.pdf
analysis/agent_scorecard/archive/scorecard-events-2026-03-24.jsonl.gz
analysis/agent_scorecard/archive/scorecard-events-2026-03-25.jsonl.gz
analysis/agent_scorecard/archive/*
docs/Spot_Bad_AI_Generated_Code.pdf
