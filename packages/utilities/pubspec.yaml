name: utilities
publish_to: none
description: Pure Dart utilities shared across workspace packages.
resolution: workspace

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  core:
    path: ../core
  equatable: ^2.1.0

dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.1
