name: storage
publish_to: none
description: Hive recovery helpers and storage primitives.
resolution: workspace

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  core:
    path: ../core
  utilities:
    path: ../utilities

dev_dependencies:
  lints: ^6.0.0
  test: ^1.31.0
