name: storage
publish_to: none
description: Hive schema, migrations, encryption keys, and storage primitives.
resolution: workspace

environment:
  sdk: ">=3.12.0 <4.0.0"
  flutter: ">=3.38.0"

dependencies:
  app_shared_flutter:
    path: ../app_shared_flutter
  core:
    path: ../core
  flutter:
    sdk: flutter
  freezed_annotation: ^3.1.0
  hive_flutter: ^1.1.0
  json_annotation: ^4.9.0
  path_provider: ^2.1.5
  shared_preferences: ^2.5.3
  utilities:
    path: ../utilities

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
