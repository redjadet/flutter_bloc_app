name: networking
publish_to: none
description: HTTP helpers, circuit breaker, and Retrofit adapters.
resolution: workspace

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  core:
    path: ../core
  dio: ^5.10.0
  retrofit: ^4.9.2
  utilities:
    path: ../utilities

dev_dependencies:
  lints: ^6.0.0
  test: ^1.31.0
