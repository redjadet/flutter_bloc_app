name: core
publish_to: none
description: Pure Dart domain primitives and time abstractions.
resolution: workspace

environment:
  sdk: ">=3.12.0 <4.0.0"

dev_dependencies:
  lints: ^6.0.0
  test: ^1.31.0
