name: feature_flags
publish_to: none
description: Remote config and feature-flag contracts.
resolution: workspace

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  core:
    path: ../core
  utilities:
    path: ../utilities

dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.1
