name: auth
publish_to: none
description: Provider-neutral auth contracts shared across apps.
resolution: workspace

environment:
  sdk: ">=3.12.0 <4.0.0"
  flutter: ">=3.38.0"

dependencies:
  core:
    path: ../core
  # Hold below FlutterFire FirebasePlugin rename (flutterfire#18437).
  firebase_auth: ">=6.5.4 <6.5.5"
  flutter:
    sdk: flutter
  supabase_flutter: ^2.16.0
  utilities:
    path: ../utilities

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.5
  test: ^1.31.1
