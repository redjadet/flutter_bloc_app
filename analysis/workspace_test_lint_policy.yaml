include: workspace_lint_policy.yaml

analyzer:
  language:
    # Tests commonly use dynamic fixture payloads and mock boundaries.
    strict-casts: false
    strict-inference: false
    strict-raw-types: false
