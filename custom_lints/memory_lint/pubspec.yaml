name: memory_lint
publish_to: 'none'
description: Native analyzer plugin for high-confidence Flutter lifecycle leaks.
version: 0.1.0

environment:
  sdk: ">=3.13.0 <4.0.0"
resolution: workspace

dependencies:
  analysis_server_plugin: 0.3.22
  analyzer: 10.0.2
  analyzer_plugin: 0.14.16

dev_dependencies:
  flutter_lints: ^6.0.0
  test: ^1.31.1
