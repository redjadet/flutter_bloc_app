/// Core-level extensions (e.g. BuildContext helpers for theme/router/config).
///
/// Responsive, bloc, and l10n extensions live in lib/shared/extensions/.
library;

// Add core-level extension exports here when needed (e.g. context.appTheme).
