/// Filter for the IoT demo device list.
enum IotDemoDeviceFilter {
  /// Show all devices.
  all,

  /// Show only devices with toggle on.
  toggledOnOnly,

  /// Show only devices with toggle off.
  toggledOffOnly,
}
