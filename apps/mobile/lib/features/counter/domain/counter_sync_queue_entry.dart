/// Summary of a pending sync operation for counter inspector UI.
class const CounterSyncQueueEntry({
  required final String id,
  required final String entityType,
  required final int retryCount,
});
