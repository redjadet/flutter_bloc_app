# Known Hive getBox() + write outside runWithBox (lost-update debt).
# Entries are path#method signatures — new unsafe methods in debt files still fail.
# Remove a signature when that method migrates to runWithBox (#834 / Codex review).
# Allowlist cleared 2026-09-13: todo/pending_sync, counter, IoT, chat, profile,
# search, chart, GraphQL, iGaming, realtime market, remote config, staff timeclock.
