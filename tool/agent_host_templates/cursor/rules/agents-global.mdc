---
description: Extra Flutter-repo depth when engaged (not always-on; pair with AGENTS.md and agent-execution.mdc).
alwaysApply: false
---

# Global Flutter agent reminder

Canon starts at `AGENTS.md` + `docs/ai/context_loading.md`. Pointers: `docs/agent_knowledge_base.md`, `docs/agent_project_context.md`, `docs/ai_code_review_protocol.md`. Commands: `docs/agents_quick_reference.md` only.

## Execution

Intent/spec/implementation separate. Vague/risky: boundaries, data flow, failures, smallest slice before codegen. Loop: plan → tool → observe → revise; bad tool output ≠ proof. Tools: repo scripts, MCP, browser/app proof — not prompt memory. UI: `DESIGN.md` + `docs/design_system.md`; states + responsive no-overlap.

## Context + quality

**context ladder:** `docs/ai/context_loading.md`; depth in `docs/agent_knowledge_base.md`. Smallest reads; landmines only on audits. **95% confident** before edit. **Surgical diff** + validation gates (fix false positives only). Draft until review; **Self-verify before report**.

## Repo-first

Repo docs/shell over host shortcuts. Repeated failure → doc/fixture/test/script. **File verified reusable conclusions** → owning doc / `docs/changes/` / `tasks/lessons.md`. **Semantic lint** on agent docs; `./tool/check_agent_knowledge_base.sh`. Source docs → templates → `./bin/agent-maintain` (`preflight` / `after-host-edit` / `closeout`). Widget tests: [`docs/testing_overview.md`](docs/testing_overview.md) + [`docs/testing/widget_test_playbook.md`](docs/testing/widget_test_playbook.md) (no repo-wide `WidgetTester.view` yet).

Flutter SDK/framework read-only: do not patch `/Users/ilkersevim/Flutter_SDK/flutter/**` or Dart/Flutter toolchain sources to fix app issues.
