---
name: agents-cursor-integration
description: Cursor integration pointer for Flutter BLoC app. Use when editing repo rules, shared Cursor adapters, or Cursor/Codex review wiring.
---

# Cursor integration

**Canon:** `AGENTS.md`, `docs/agent_knowledge_base.md`, `docs/agents_quick_reference.md`, `docs/ai_code_review_protocol.md`, `docs/agent_host_notes.md`. Source: `tool/agent_host_templates/cursor/**`. `./tool/sync_agent_assets.sh`, `./tool/check_agent_asset_drift.sh`. `tasks/cursor/team/<run-id>/`; `agent_knowledge_base.md#multi-agent-hub`. Sync manages project-only `agent-execution.mdc` in `.cursor/rules/`. **Skills:** sync to `~/.cursor/skills` only — do not copy managed skill names into repo `.cursor/skills/` (duplicate picker entries).
